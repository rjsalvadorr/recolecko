{
  "name": "recolecko",
  "version": "1.0.0",
  "description": "A file manager for creatives that work fast",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-app": "electron .",
    "build-app": "electron-packager . recolecko --platform=win32,linux,darwin --out=build --overwrite"
  },
  "author": "RJ Salvador",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "moment": "^2.21.0",
    "rimraf": "^2.6.2",
    "sentencer": "^0.1.5"
  },
  "devDependencies": {
    "electron": "^1.8.4",
    "electron-packager": "^12.0.1"
  },
  "electronPackagerConfig": {
    "icon": "img/icon.ico"
  }
}
